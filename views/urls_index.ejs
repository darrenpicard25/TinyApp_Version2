<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
  <%- include ./partials/_header.ejs %>
    <!-- Add EJS code here -->
    <br>
    <h3>My URLS:</h3>
    <table>

    <% for (let url in urls) { %>
  <tr>
 <td><strong><%=` ${url}` %></strong><%= "------->" %><a href= <%='/u/'+url %> ><%= `${urls[url]}` %></a></td>
  <td> &nbsp&nbsp&nbsp&nbsp </td>
  <td><form method="GET" class="inline-form" action=<%=`/urls/${url}`%>>
      <button type="submit" class="btn btn-info">Edit</button></form></td>
   <td><form method="POST" class="inline-form" action=<%=`/urls/${url}/delete`%>>
      <button type="submit" class="btn btn-danger">Delete</button>
    </form></td>
  </tr>

    <%}%>
</table>
</body>
</html>