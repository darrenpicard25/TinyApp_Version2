<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
    <%- include ./partials/_header.ejs %>
<br>
    <!-- Add EJS code here -->
<h4><strong><%= ` ${shortURL}` %></strong></h4>

  <form class="form-inline" action=<%=`/urls/${shortURL}?_method=PUT`%> method="POST">
  <label for="longURL">URL:</label>
  <input class="form-control" type="text" name="newlongURL" value=<%=longURL%> placeholder="http://" style="width: 300px; margin: 1em">
  <button type="submit" class="btn btn-primary">Change URL</button>
  </form>
  <hr>
  <h5>Number of times used : <%=timesUsed%></h5>
  <hr>
  <h5>Number of Unique visits : <%=uniqueTimes%></h5>
  <hr>
  <h3 style="font-weight: bold; text-decoration: underline;">Time Stamps</h3>
  <table class='table'>
    <thead class="thead-dark">
    <tr>
      <th>Date</th>
      <th>ID of Individual</th>
    </tr>
  </thead>
  <% for (stamp of timeStamps) { %>
    <tr>
      <td><%= stamp[0]%></td>
      <td><%= stamp[1]%></td>
    </tr>
    <% } %>

</body>
</html>